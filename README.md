# aws-eks-compliance-checker
A lightweight AWS EKS compliance checker built using AWS CLI and shell scripting. It validates basic CIS-aligned controls like public access, logging, and secrets encryption, and exports results to a CSV report.
